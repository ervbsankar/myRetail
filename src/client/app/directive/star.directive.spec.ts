import { AppStarDirective } from "./star.directive";

describe("AppStarDirective", () => {
  it("should create an instance", () => {
    const directive = new AppStarDirective();
    expect(directive).toBeTruthy();
  });
});
